# Vidéo Hero (optionnelle)

La V1 utilise `../images/hero-impact.webp` pour garantir un chargement rapide.
Placez ici la future vidéo Hero (par exemple `hero-full-impact.mp4`) puis suivez les instructions du README principal pour l'intégrer avec une image `poster`.
